<UserControl x:Class="DesktopFences.UI.Controls.Settings.GeneralSettingsPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <!-- Pane header -->
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="常规" Style="{StaticResource SwPaneTitleStyle}" />
            <TextBlock Text="启动、行为与基本偏好设置。"
                       Style="{StaticResource SwPaneDescStyle}" />
        </StackPanel>

        <!-- 行为 -->
        <TextBlock Text="行为" Style="{StaticResource SwSectionHeaderStyle}" Margin="0,0,0,10" />

        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="吸附距离 (像素)" Foreground="{DynamicResource TextSecondaryBrush}"
                       VerticalAlignment="Center" />
            <Slider x:Name="SliderSnapThreshold" Grid.Column="1"
                    Style="{DynamicResource DarkSliderStyle}"
                    Minimum="0" Maximum="30" TickFrequency="5"
                    IsSnapToTickEnabled="True" VerticalAlignment="Center" />
        </Grid>

        <CheckBox x:Name="ChkQuickHide" Content="启用快速隐藏（双击桌面）"
                  Style="{DynamicResource DarkCheckBoxStyle}" Margin="0,0,0,8" />

        <!-- 启动 -->
        <TextBlock Text="启动" Style="{StaticResource SwSectionHeaderStyle}" Margin="0,16,0,10" />

        <CheckBox x:Name="ChkStartWithWindows" Content="随 Windows 启动"
                  Style="{DynamicResource DarkCheckBoxStyle}" Margin="0,0,0,8" />

        <CheckBox x:Name="ChkStartMinimized" Content="启动后最小化到托盘"
                  Style="{DynamicResource DarkCheckBoxStyle}" Margin="0,0,0,8" />
    </StackPanel>
</UserControl>
